Type: "AWS::AppSync::GraphQLApi"
Properties:
  UserPoolConfig:
    UserPoolId:
      Ref: userPool
    AwsRegion: ${self:provider.region}
    DefaultAction: ALLOW
  Name: ${self:service}-graphql-api
  AuthenticationType: AMAZON_COGNITO_USER_POOLS
