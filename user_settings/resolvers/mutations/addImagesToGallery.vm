{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "id" : $util.dynamodb.toDynamoDBJson($ctx.args.id)
    },
    "update" : {
        "expression" : "SET images = list_append(images, :image_ids)",
        "expressionValues": {
            ":image_ids" : $util.dynamodb.toDynamoDBJson($ctx.args.image_ids)
        }
    }
}